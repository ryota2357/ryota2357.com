---
type Props = {
  title: string;
};

const { title } = Astro.props;
---

<div class="flex flex-col items-start gap-4">
  <h2 class="text-[1.6rem] font-bold">{title}</h2>
  <div class="flex w-full flex-col gap-4 pl-4 text-[1rem]">
    <slot />
  </div>
</div>
